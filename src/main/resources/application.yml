spring:
  application:
    name: nullang
spring:
  output:
    ansi:
      enabled: ALWAYS
logging:
  level:
    root: INFO
  pattern:
    console: "%clr(%d{HH:mm:ss.SSS}){faint} %clr(%5p) %clr([%t]){faint} %clr(%-40.40logger{39}){cyan} : %m%n%wex"
